# Local dev example
RDS_HOST=localhost
RDS_PORT=5432
RDS_DB=coffee_rag
RDS_USER=postgres
RDS_PASSWORD=postgres

# AWS Bedrock (Lambda proxy expects these keys)
BEDROCK_REGION=ap-northeast-1

# Prefer inference profile over modelId for Anthropic models (required for 3.7/4)
GENERATION_INFERENCE_PROFILE_ARN=arn:aws:bedrock:ap-northeast-1:010922940107:inference-profile/apac.anthropic.claude-3-7-sonnet-20250219-v1:0

# Default aliases for auto-resolution (optional)
DEFAULT_GENERATION_ALIAS=claude-3.7-sonnet
DEFAULT_EMBEDDING_ALIAS=titan-v2-1024

# If you want to pin model IDs explicitly, uncomment:
# GENERATION_MODEL_ID=
EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0

# Notes:
# - Titan v2 default is 1024-dim embeddings; align pgvector as vector(1024).
# - Leave GENERATION_MODEL_ID empty to always use the profile above.